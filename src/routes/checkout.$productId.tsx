import { createFileRoute, Link, useRouter } from '@tanstack/react-router'
import { ArrowLeft, CheckCircle2, LoaderCircle, LockKeyhole, ShieldCheck, Smartphone, Zap } from 'lucide-react'
import { useEffect, useState } from 'react'
import { broadcastAccessChanged, paymentStatus, publicCatalogue, recoverPayment, resolveCatalogueKey, startPalplussPayment } from '../lib/avant-backend'
import { customerSession, rememberedCustomer, requireCustomerToken, signInCustomer, watchCustomerSession } from '../lib/google-auth'
import { productForLegacyContent } from '../lib/backend-catalogue-map'

export const Route=createFileRoute('/checkout/$productId')({head:()=>({meta:[{name:'robots',content:'noindex, nofollow'}]}),component:CheckoutRoute})

function CheckoutRoute(){
 const{productId}=Route.useParams(),router=useRouter()
 const qs=typeof window==='undefined'?new URLSearchParams():new URLSearchParams(window.location.search),embedded=qs.get('embedded')==='1',returnTo=qs.get('returnTo')||'',origin=qs.get('origin')||returnTo||'/movies',originScroll=Number(qs.get('originScroll')||0)
 const[mobile,setMobile]=useState(''),[paymentKey,setPaymentKey]=useState(''),[mpesaCode,setMpesaCode]=useState(''),[recoveryOpen,setRecoveryOpen]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState(''),[product,setProduct]=useState<any>(null),[loading,setLoading]=useState(true),[customer,setCustomer]=useState<any>(null),[authBusy,setAuthBusy]=useState(false),[payStage,setPayStage]=useState<'idle'|'sending'|'phone'|'confirming'>('idle'),[reference,setReference]=useState('')
 useEffect(()=>{void customerSession().then(setCustomer);return watchCustomerSession(()=>void customerSession().then(setCustomer))},[])
 useEffect(()=>{try{const saved=sessionStorage.getItem(`avant_payment_${productId}`)||'';if(saved)setReference(saved)}catch{}},[productId])
 useEffect(()=>{let active=true;const mapped=productForLegacyContent(productId)||productId;setLoading(true);Promise.allSettled([resolveCatalogueKey(productId),publicCatalogue()]).then(results=>{if(!active)return;const resolved=results[0].status==='fulfilled'?results[0].value?.product:null;const list=results[1].status==='fulfilled'?(results[1].value?.products||[]):[];setProduct(resolved||list.find((p:any)=>p.id===mapped)||null);setLoading(false)});return()=>{active=false}},[productId])
 async function loginFirst(){setAuthBusy(true);setError('');try{setCustomer(await signInCustomer(rememberedCustomer()?.email))}catch(e:any){setError(e?.message||'Sign-in failed')}finally{setAuthBusy(false)}}
 async function start(){if(!customer){setError('Sign in first to continue to M-PESA.');return}setPayStage('sending');setBusy(true);setError('');await new Promise<void>(resolve=>requestAnimationFrame(()=>requestAnimationFrame(()=>resolve())));try{const token=await requireCustomerToken();const resolved=product?.id||productForLegacyContent(productId)||productId;let key=paymentKey;try{key=key||sessionStorage.getItem(`avant_payment_key_${productId}`)||''}catch{}if(!key){key=crypto.randomUUID();setPaymentKey(key);try{sessionStorage.setItem(`avant_payment_key_${productId}`,key)}catch{}}const result=await startPalplussPayment(token,{productId:resolved,phone:mobile,idempotencyKey:key});const reference=result.reference||result.payment?.reference;if(!reference)throw new Error(result.message||'Payment was not started.');setReference(reference);try{sessionStorage.setItem(`avant_payment_${productId}`,reference)}catch{}setPayStage('phone');window.setTimeout(()=>setPayStage('confirming'),1200);if(embedded){let attempts=0;const poll=async()=>{attempts++;try{const freshToken=await requireCustomerToken();const r=await paymentStatus(freshToken,reference);const s=String(r.payment?.status||r.status||'').toLowerCase();if(['successful','success','completed','paid'].includes(s)||(r.entitlements?.length??0)>0){try{sessionStorage.removeItem(`avant_payment_${productId}`);sessionStorage.removeItem(`avant_payment_key_${productId}`)}catch{}setPayStage('idle');setBusy(false);broadcastAccessChanged({reference,source:'checkout'});window.parent.postMessage({type:'avant-payment-success',reference,returnTo},window.location.origin);return}if(s==='failed'||s==='refunded'||s==='cancelled'){setBusy(false);setPayStage('idle');setError('The payment was not completed.');return}}catch(e:any){if(attempts%5===0)setError('Payment sent. Still checking confirmation…')}if(attempts<120)window.setTimeout(poll,2000);else{setBusy(false);setPayStage('idle');setError('Payment was sent but confirmation is delayed. Use Check payment below; do not pay again.')}};void poll();return}await router.navigate({to:'/payment/success',search:{reference,returnTo:returnTo||origin,origin,originScroll:String(originScroll)}})}catch(e:any){setPayStage('idle');setError(e?.message||'Unable to start payment');setBusy(false)}}

 async function recoverPaid(){if(!customer){setError('Sign in first so Avant can match the payment to your account.');return}if(!mobile.trim()&&!mpesaCode.trim()){setError('Enter the M-PESA phone number or transaction code.');return}setBusy(true);setPayStage('confirming');setError('');try{const token=await requireCustomerToken();const r=await recoverPayment(token,{phone:mobile.trim()||undefined,mpesaCode:mpesaCode.trim()||undefined});if(r.confirmed){setReference(r.reference||'');try{sessionStorage.removeItem(`avant_payment_${productId}`)}catch{}setBusy(false);setPayStage('idle');broadcastAccessChanged({reference:r.reference,source:'recovery'});if(embedded)window.parent.postMessage({type:'avant-payment-success',reference:r.reference,returnTo},window.location.origin);else await router.navigate({to:'/payment/success',search:{reference:r.reference,returnTo:returnTo||origin,origin,originScroll:String(originScroll)}});return}setBusy(false);setPayStage('idle');setError(r.message||'No matching successful payment has been confirmed yet.')}catch(e:any){setBusy(false);setPayStage('idle');setError(e?.message||'Unable to verify payment right now.')}}
 async function checkPayment(){let ref=reference;try{ref=ref||sessionStorage.getItem(`avant_payment_${productId}`)||''}catch{}if(!ref){setError('No payment reference is saved on this device. Do not pay again; contact support with your M-PESA message.');return}setBusy(true);setPayStage('confirming');setError('');try{const token=await requireCustomerToken();const r=await paymentStatus(token,ref);const s=String(r.payment?.status||r.status||'').toLowerCase();if(['successful','success','completed','paid'].includes(s)||(r.entitlements?.length??0)>0){try{sessionStorage.removeItem(`avant_payment_${productId}`)}catch{}setBusy(false);setPayStage('idle');broadcastAccessChanged({reference:ref,source:'payment-check'});if(embedded)window.parent.postMessage({type:'avant-payment-success',reference:ref,returnTo},window.location.origin);else await router.navigate({to:'/payment/success',search:{reference:ref,returnTo:returnTo||origin,origin,originScroll:String(originScroll)}});return}setBusy(false);setPayStage('idle');setError('Payment has not been confirmed by the server yet. Do not pay again; retry this check shortly.')}catch(e:any){setBusy(false);setPayStage('idle');setError(e?.message||'Unable to check payment right now. Do not pay again.')}}
 const payableMinor=product?(product.promotional_price_minor??product.price_minor):0;const amount=product?`${product.currency} ${(payableMinor/100).toLocaleString()}`:'—';const regularAmount=product&&product.promotional_price_minor!=null?`${product.currency} ${(product.price_minor/100).toLocaleString()}`:''
 return <main className={`${embedded?"min-h-full":"min-h-[100svh]"} bg-[#050608] text-white`}>
  <div className={`mx-auto w-full ${embedded?"max-w-2xl px-4 pb-[max(1rem,env(safe-area-inset-bottom))] pt-3 sm:px-6 sm:py-6":"max-w-6xl px-4 py-4 sm:px-6 sm:py-7 lg:px-8 xl:px-10"}`}>
   <header className={`${embedded?"hidden ":""}flex items-center justify-between border-b border-white/10 pb-4`}>
    <button type="button" onClick={()=>{if(window.history.length>1)window.history.back();else window.location.assign(new URL((returnTo||origin).replace(/^\//,""),document.baseURI).href)}} className="inline-flex min-h-11 items-center gap-2 text-sm font-semibold text-white/70 transition hover:text-white"><ArrowLeft className="size-4"/>Back</button>
    <div className="inline-flex items-center gap-2 text-xs font-semibold uppercase tracking-[.14em] text-emerald-300"><LockKeyhole className="size-4"/>Secure checkout</div>
   </header>
   <div className={embedded?"mx-auto max-w-xl pb-3 pt-0":"grid gap-5 py-4 md:py-6 lg:grid-cols-[minmax(0,1fr)_22rem] lg:items-start lg:gap-10 xl:grid-cols-[minmax(0,1fr)_24rem] xl:gap-14"}>
    <section className={embedded?"":"min-w-0 lg:max-w-2xl"}>
     <p className="text-center text-[10px] font-bold uppercase tracking-[.2em] text-orange-400 sm:text-left sm:text-xs">Complete your access</p>
     <h1 className="mx-auto mt-2 max-w-2xl text-center text-2xl font-black tracking-[-.03em] sm:mx-0 sm:text-left sm:text-4xl">Unlock. Pay. <span className="text-orange-400">Start watching.</span></h1>
     <p className="mx-auto mt-2 max-w-xl text-center text-sm leading-5 text-white/55 sm:mx-0 sm:text-left sm:text-base">Pay securely with M-PESA. Access is attached to your Google account after the payment is verified.</p>
     <div className="mx-auto mt-3 max-w-xl rounded-2xl border border-white/10 bg-white/[.035] p-4 sm:mx-0"><p className="text-[10px] font-bold uppercase tracking-[.18em] text-white/40">Step 1 · Account</p>{customer?<div className="mt-2 flex items-center gap-3"><CheckCircle2 className="size-5 text-emerald-300"/><div><p className="text-sm font-bold">{customer.name||"Signed in"}</p><p className="text-xs text-white/45">{customer.email}</p></div></div>:<button onClick={()=>void loginFirst()} disabled={authBusy} className="mt-3 min-h-12 w-full rounded-xl bg-white px-5 text-sm font-bold text-black hover:bg-white/90">{authBusy?"Signing in…":"Continue with Google"}</button>}</div><div className="mx-auto mt-3 max-w-xl overflow-hidden rounded-2xl sm:mx-0 sm:rounded-3xl border border-white/10 bg-gradient-to-b from-white/[.07] to-white/[.025] shadow-2xl"><div className="border-b border-white/10 px-4 py-3 sm:px-5"><p className="flex items-center gap-2 text-sm font-bold"><Zap className="size-4 text-orange-400"/>M-PESA Express</p><p className="mt-1 text-xs text-white/40">Secure STK push · usually takes a few seconds</p></div><div className="p-4 sm:p-5">
      <p className="mb-4 text-[10px] font-bold uppercase tracking-[.18em] text-white/40">Step 2 · Payment</p><label htmlFor="mpesa-number" className="text-sm font-bold">M-PESA mobile number</label>
      <p className="mt-1 text-xs text-white/45">Use the number that should receive the STK push.</p>
      <div className="mt-3 flex min-h-12 items-center rounded-xl border border-white/15 bg-black/35 px-4 focus-within:border-white/40 focus-within:ring-2 focus-within:ring-white/10">
       <span className="mr-3 border-r border-white/10 pr-3 text-sm font-semibold text-white/45">KE</span>
       <input id="mpesa-number" value={mobile} onChange={e=>setMobile(e.target.value)} placeholder="07XX XXX XXX" inputMode="tel" autoComplete="tel" className="min-w-0 flex-1 bg-transparent text-base font-semibold outline-none placeholder:text-white/25"/>
      </div>
      <button onClick={()=>void start()} disabled={!customer||busy||mobile.trim().length<9||loading} className={`mt-3 flex min-h-12 w-full items-center justify-center gap-3 rounded-xl px-5 font-bold transition ${busy?'bg-white/85 text-black cursor-wait':'bg-white text-black hover:bg-white/90'} disabled:cursor-not-allowed disabled:opacity-60`}>
       {busy?<span className="inline-flex size-6 shrink-0 animate-spin rounded-full border-[3px] border-black/25 border-t-black" aria-label="Loading"/>:<Smartphone className="size-5"/>}<span>{payStage==='sending'?'Sending M-PESA prompt…':payStage==='phone'?'Check your phone…':payStage==='confirming'?'Confirming payment…':`Pay ${amount} with M-PESA`}</span>
      </button>
      <button type="button" onClick={()=>setRecoveryOpen(v=>!v)} disabled={busy||!customer} className="mt-2 min-h-11 w-full rounded-xl border border-white/15 px-4 text-sm font-semibold text-white/80 disabled:opacity-50">I already paid · Verify payment</button>
       {recoveryOpen?<div className="mt-3 rounded-xl border border-white/10 bg-black/25 p-3"><p className="text-xs font-semibold text-white/70">Verify using the M-PESA number or transaction code</p><input value={mpesaCode} onChange={e=>setMpesaCode(e.target.value.toUpperCase())} placeholder="M-PESA transaction code" autoCapitalize="characters" className="mt-2 min-h-11 w-full rounded-lg border border-white/15 bg-black/35 px-3 text-sm outline-none placeholder:text-white/30"/><button type="button" onClick={()=>void recoverPaid()} disabled={busy||(!mobile.trim()&&!mpesaCode.trim())} className="mt-2 min-h-11 w-full rounded-lg bg-white px-4 text-sm font-bold text-black disabled:opacity-50">{busy?'Verifying…':'Verify paid transaction'}</button><p className="mt-2 text-[11px] leading-4 text-white/40">Avant only grants access after Palpluss confirms the transaction, amount and status.</p></div>:null}
       {reference?<button type="button" onClick={()=>void checkPayment()} disabled={busy||!customer} className="mt-2 min-h-10 w-full text-xs font-semibold text-white/55 disabled:opacity-50">Check saved Avant reference</button>:null}
      <p className="mt-2 text-center text-[11px] leading-4 text-white/40">{payStage==='sending'?'Please wait. We’re sending an M-PESA prompt to your phone.':payStage==='phone'?'Check your phone and enter your M-PESA PIN.':payStage==='confirming'?'Payment sent. We’re confirming it automatically…':'You will confirm the payment on your phone. We never ask for your M-PESA PIN on this website.'}</p>
      {error&&<div className="mt-5 rounded-xl border border-red-400/20 bg-red-400/10 p-4 text-sm leading-6 text-red-100">{error}</div>}
     </div></div>
     <div className={`${embedded?"mt-3":"mt-4"} mx-auto grid max-w-xl grid-cols-3 gap-2 sm:mx-0 sm:gap-3`}>
      {['Sign in','Pay with M-PESA','Start watching'].map((label,i)=><div key={label} className="flex min-w-0 flex-col items-center gap-1.5 rounded-xl border border-white/10 px-2 py-2.5 text-center text-[10px] font-semibold text-white/65 sm:flex-row sm:gap-3 sm:px-4 sm:py-3 sm:text-left sm:text-xs"><span className="grid size-6 shrink-0 place-items-center rounded-full bg-white/10 text-[11px] text-white">{i+1}</span>{label}</div>)}
     </div>
    </section>
    {!embedded?<aside className="h-fit overflow-hidden rounded-2xl border border-white/10 bg-[#0d0f13] shadow-2xl sm:rounded-3xl lg:sticky lg:top-24"><div className="h-1 bg-gradient-to-r from-orange-500 via-orange-300 to-transparent"/><div className="p-4 sm:p-6">
     <p className="text-xs font-bold uppercase tracking-[.18em] text-white/40">Order summary</p>
     {loading?<div className="mt-5 h-24 animate-pulse rounded-xl bg-white/5"/>:product?<><h2 className="mt-5 text-xl font-bold">{product.name}</h2><p className="mt-2 text-sm leading-6 text-white/50">{product.description||'Avant Movies streaming access'}</p><div className="my-6 border-t border-white/10"/><div className="flex items-end justify-between gap-4"><span className="text-sm text-white/55">Total</span><div className="text-right">{regularAmount?<p className="text-xs text-white/35 line-through">{regularAmount}</p>:null}<strong className="text-2xl">{amount}</strong></div></div><p className="mt-2 text-right text-xs text-white/40">{product.duration_days} days access</p></>:<p className="mt-5 text-sm text-white/55">Access option will be confirmed securely before payment.</p>}
     <div className="mt-7 space-y-3 border-t border-white/10 pt-5 text-xs text-white/50">
      <p className="flex gap-2"><ShieldCheck className="size-4 shrink-0 text-emerald-300"/>Price is verified by Avant's server.</p>
      <p className="flex gap-2"><CheckCircle2 className="size-4 shrink-0 text-emerald-300"/>Access is activated only after payment confirmation.</p>
     </div>
    </div></aside>:<details className="mx-auto mt-4 max-w-xl rounded-2xl border border-white/10 bg-[#0d0f13]"><summary className="flex cursor-pointer list-none items-center justify-between px-4 py-3 text-sm font-semibold"><span>Order summary</span><span className="text-white/55">{amount}</span></summary><div className="border-t border-white/10 px-4 py-4"><p className="font-semibold">{product?.name||"Avant access"}</p><p className="mt-1 text-xs text-white/45">{product?.duration_days?product.duration_days+" days access":"Access details verified before payment"}</p></div></details>}
   </div>
  </div>
 </main>
}
