const PROJECT="avant-movies-b6b94";const API_KEY=(import.meta.env.VITE_FIREBASE_API_KEY as string|undefined)?.trim();const APP_URL="https://www.gstatic.com/firebasejs/12.3.0/firebase-app.js";const AUTH_URL="https://www.gstatic.com/firebasejs/12.3.0/firebase-auth.js";let mod:any,auth:any;
async function ready(){if(!API_KEY)throw new Error("Sign-in is not configured for this build.");const appMod:any=await import(/* @vite-ignore */ APP_URL);mod=await import(/* @vite-ignore */ AUTH_URL);const app=appMod.getApps().length?appMod.getApp():appMod.initializeApp({apiKey:API_KEY,authDomain:`${PROJECT}.firebaseapp.com`,projectId:PROJECT});auth=mod.getAuth(app);return mod}
export async function customerToken(interactive=false){const m=await ready();await auth.authStateReady();let u=auth.currentUser;if(!u&&interactive){const result=await m.signInWithPopup(auth,new m.GoogleAuthProvider());u=result.user}return u?u.getIdToken(true):null}
export async function requireCustomerToken(){const t=await customerToken(true);if(!t)throw new Error("Please sign in to continue.");return t}
export async function customerEmail(){await ready();await auth.authStateReady();return auth.currentUser?.email??null}
